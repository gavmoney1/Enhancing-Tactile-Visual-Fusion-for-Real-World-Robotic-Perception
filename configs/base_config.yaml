# Base configuration for all models
data:

  orig_root: "C:/Users/gabri/OneDrive/Documents/Fall25/Capstone/Development/matched_frames/20220318_021533"
  mask_root: "C:/Users/gabri/OneDrive/Documents/Fall25/Capstone/Development/pca_masked_frames/20220318_021533"
  labels_path: "/your/labels.csv/here" # TODO: add label support, not currently implemented
  out_dir: "C:/Users/gabri/OneDrive/Documents/Fall25/Capstone/Development/test_output"
  img_size: 224
  mask_ratio: 0.9  # 90% masked
  
training:
  batch_size: 8  # Reduced for laptop
  epochs: 1
  val_split: 0.10
  test_split: 0.20 # TODO: these vals currently only affect metric report
  seed: 42
  
optimization:
  learning_rate: 1e-4
  weight_decay: 1e-5
  warmup_steps: 1000
  
memory:
  enable_mixed_precision: true
  gradient_accumulation_steps: 2
  max_memory_gb: 8  # Adjust based on your laptop
  
models_to_train:
  - "mae_up"
  - "conv_autoencoder"
  - "vit"
  - "swin" 
  - "detr"
  - "rad"
  
evaluation:
  save_predictions: true
  num_visualization_samples: 4
